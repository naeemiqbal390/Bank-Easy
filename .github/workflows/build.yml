name: Build Android APK & AAB

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: bankeasy_app
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: Generate Android platform files
        run: flutter create . --platforms=android --org com.bankeasy --project-name bankeasy

      - name: Install dependencies
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release

      - name: Build release AAB
        run: flutter build appbundle --release

      - name: Upload build outputs
        uses: actions/upload-artifact@v4
        with:
          name: bankeasy-release-build
          path: |
            bankeasy_app/build/app/outputs/flutter-apk/app-release.apk
            bankeasy_app/build/app/outputs/bundle/release/app-release.aab
